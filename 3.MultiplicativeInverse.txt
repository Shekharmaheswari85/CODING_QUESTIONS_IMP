@csalgo

Problem Name: Multiplicative Inverse
Problem Difficulty: 
Problem Constraints: 1<=n<=1000000
Problem Description:
Given a number n. Print the multiplicative inverse of “n modulo 1000000007” .


Input Format: Single line containing an integer value.
Sample Input: 3

Output Format: Integral value representing the multiplicative inverse.
Sample Output: 333333336



=====Solution=====
/*input
198
*/
#include <bits/stdc++.h>
using namespace std;
 
#define boost  ios_base::sync_with_stdio(false);
#define endl '\n'
#define mp make_pair
#define pb push_back
#define fi first
#define se second
#define ll long long
#define ull unsigned long long
#define max(a,b) ((a>b)?(a):(b))
#define min(a,b) ((a<b)?(a):(b))
 
//values
#define MAX 100005
#define inf LLONG_MAX
#define MIN INT_MIN
#define PIE 3.141592653589793238
 
ll mod=1e9+7;
ll gcd(ll a , ll b){return b==0?a:gcd(b,a%b);}
ll powmod(ll a,ll b) {ll res=1;if(a>=mod)a%=mod;for(;b;b>>=1){if(b&1)res=res*a;if(res>=mod)res%=mod;a=a*a;if(a>=mod)a%=mod;}return res;}
ull mul_ab_mod_c(ull a, ull b, ull c){ull x = 0,y = a%c; while(b>0){if(b&1) x = (x+y)%c;y = (y<<1)%c;b >>= 1;}return x;}


int main()
{
    ll n;
    cin>>n;
    cout<<powmod(n,mod-2);
    return 0;
}

@csalgo Telegram